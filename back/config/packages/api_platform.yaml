api_platform:
  title: API portfolio john haimez
  description: Gestion des données pour le site personnel de john haimez
  version: 0.0.1
  formats:
    json: ['application/json']
    jsonld: ['application/ld+json']
    html: ['text/html']
    multipart: ['multipart/form-data']
  patch_formats:
    json: ['application/merge-patch+json']
  swagger:
    api_keys:
      JWT:
        name: Authorization
        type: header
