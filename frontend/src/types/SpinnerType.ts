/**
 * type for spinner circle
 */
export type TspinnerCircle = {
  size?: string
}

/**
 * type for back of spinner global
 */
export type TbackLoader = {
  children: JSX.Element | JSX.Element[] | null
}
